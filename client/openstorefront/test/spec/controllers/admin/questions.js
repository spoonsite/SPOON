/*'use strict';

describe('Controller: AdminQuestionsCtrl', function () {

  // load the controller's module
  beforeEach(module('openstorefrontApp'));

  var AdminQuestionsCtrl,
    scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller, $rootScope) {
    scope = $rootScope.$new();
    AdminQuestionsCtrl = $controller('AdminQuestionsCtrl', {
      $scope: scope
    });
  }));

  it('should have the correct initializations', function () {
    expect(scope.predicate).toEqual([]);
    expect(scope.reverse).toEqual([]);
    expect(scope.statusFilterOptions).toEqual({code: 'A', desc: 'Active'}, {code:'I', desc: 'Inactive'});
  });
  alert('Controller:  AdminQuestionsCtrl; should have the correct initializations = PASS (3 expects)');
});
*/

