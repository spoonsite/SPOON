/*'use strict';

describe('Controller: SavecompconfCtrl', function () {

// load the controller's module
  beforeEach(module('openstorefrontApp'));

  var SavecompconfCtrl,
    scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller, $rootScope) {
    scope = $rootScope.$new();
    SavecompconfCtrl = $controller('SavecompconfCtrl', {
      $scope: scope
    });
  }));

  it('should have the correct initializations', function () {
    expect(scope.componentCron).toEqual('Using Global');
    expect(scope.config).toEqual(null);
  });
  alert('Controller:  SavecompconfCtrl; should have the correct initializations = PASS (?? expects)');
});
*/